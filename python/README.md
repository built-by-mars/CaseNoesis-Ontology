# case-uco

Python bindings for constructing and serializing CASE/UCO ontology graphs.

Install from this directory:

```bash
python -m pip install .
python -m pip install -e .
```

See the [CASE/UCO SDK repository](https://github.com/vulnmaster/CASE-UCO-SDK) for documentation, recipes, and release notes.
