# CQ-37 through CQ-40 disposition — promotion, routing, relationship governance

**Date:** 2026-07-12  
**Do not tag / do not push** (per directive).

## CQ disposition

| CQ | Status | Implementation |
|----|--------|----------------|
| **CQ-37** | Done | `knowledge_lifecycle._commit_recipe_promotion`: exclusive flock on `docs/recipes/.promotion.lock`, stage under `.promotion-staging/<slug>/`, `os.replace` finals, restore backups on failure. |
| **CQ-38** | Done | `tools.run_recipe_examples.entries_for_promotion_gate`; `promote_recipe` runs candidate + shared extension/profile exemplars before catalog writes. |
| **CQ-39** | Done | Router display→ID map built from `PROFILE_REGISTRY`; missing extensions yield `validation_bundle_preview.unavailable` with `required_extensions_missing` (no silent drop). |
| **CQ-40** | Done | `mcp_server/relationship_kinds.json` (139 ObservableObjectRelationshipVocab members) + `lint_relationship_kinds`; `OrderedRoutingRecommendations` typed model. |
