# CQ-01 through CQ-12 — Recipe execution gate

**Date:** 2026-07-12  
**Scope:** ChatGPT Pro findings CQ-01–CQ-12 for `run_recipe_examples.py` / `recipe-execution.json`.  
**Release:** Not tagged.

## Disposition

| ID | Finding | Status |
|----|---------|--------|
| CQ-01 | Per-entry exception containment | **Done** — `run_entry` wraps body; typed `unhandled:…` results |
| CQ-02 | Report on all outcomes | **Done** — `main()` writes via `finally` |
| CQ-03 | Atomic report write | **Done** — temp file + `os.replace`; parents created |
| CQ-04 | Delay `--keep-generated` | **Done** — copy only after all gates pass |
| CQ-05 | Strong negative fixtures | **Done** — require `verification_status==complete`, non-conformance, and expected violation/constraint/failure code; validator missing fails entry |
| CQ-06 | Binding comparison | **Done** — canonical `expected_bindings` required; counts optional only |
| CQ-07 | Manifest JSON Schema | **Done** — `docs/recipes/recipe-execution.schema.json` enforced at load |
| CQ-08 | Path containment | **Done** — `resolve_repo_path` rejects escapes |
| CQ-09 | Support files/dirs | **Done** — `support_files` / `support_dirs` copied into temp workspace |
| CQ-10 | Exact output path | **Done** — `tmp / basename(output)` only; no `rglob` fallback |
| CQ-11 | Portable PYTHONPATH | **Done** — `os.pathsep`; records `python_executable` / `pythonpath` |
| CQ-12 | Reproducibility metadata | **Done** — timings, versions, fingerprint, sha256, exit codes, sanitized command |

## Notes

- Negative fixtures in the manifest are now objects (not bare strings).
- `GraphValidationReport.validator_diagnostics` carries truncated `case_validate` output for identifier matching.
- Builders already write `Path(__file__).parent / OUTPUT_NAME`; no builder edits required.

## Verification

```bash
PYTHONPATH=python:mcp_server python3 -m pytest mcp_server/tests/test_recipe_execution.py -v
PYTHONPATH=python:mcp_server python3 mcp_server/tools/run_recipe_examples.py \
  --id foundational-typing-bfo --validate --report /tmp/recipe-report.json
```
