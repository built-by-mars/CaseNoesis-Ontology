# Core SDK dependencies
rdflib>=7.0.0
cdo-local-uuid>=0.5.0

# Code generator
setuptools>=68.0
wheel

# Validation (case_validate CLI)
case-utils>=0.15.0

# MCP server — pinned to FastMCP 3.4.4 (Context.sample string/SamplingMessage API)
fastmcp==3.4.4

# Dev / testing
pytest>=7.0.0
mypy>=1.0,<2.4
